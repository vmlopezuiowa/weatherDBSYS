@{
    ViewData["Title"] = "Account";
}

    <p>
        <h2>Account</h2>
    </p>
        <p>
            <h3>Hello user!</h3>
            <hr />
        </p>
        <div class="form-group">
            <button type="submit" class="btn btn-default" onclick="location.href='@Url.Action("Login","Account")'"><b>Account Info</b></button>
        </div>

        <p>
            <div class="form-group">
                <button type="submit" class="btn btn-default" onclick="location.href='@Url.Action("Login","Account")'"><b>Preferences</b></button>

            </div>
        </p>
