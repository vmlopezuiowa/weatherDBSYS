@{
    ViewData["Title"] = "Account";
}

    <p>
        <h2>Account</h2>
    </p>
        <div class="form-group">
            <button type="submit" class="btn btn-default" onclick="location.href='@Url.Action("Index","Manage")'"><b>Account Info</b></button>
        </div>

        <p>
            <div class="form-group">
                <button type="submit" class="btn btn-default" onclick="location.href='@Url.Action("Index","Manage")'"><b>Preferences</b></button>

            </div>
        </p>
